; @vercel/analytics declares optional peer deps on other frameworks (SvelteKit,
; etc.) that npm's strict resolver tries to satisfy against our Vite 5 toolchain,
; producing a spurious ERESOLVE. legacy-peer-deps restores npm v6 behaviour so
; both local installs and Vercel's build succeed.
legacy-peer-deps=true
